extends base.pug

block content
	//- div.upload__container
	//- 	div.upload__video
	//- 		video#preview
	//- 	if errorMessage
	//- 		span=errorMessage
	form(method="POST" enctype="multipart/form-data")
		label(for="video") 비디오
		input(type="file" accept="video/*", required, id="video" name="video")
		label(for="thumb") 썸네일
		input(type="file" accept="image/*", required, id="thumb" name="thumb")
		input(placeholder="제목", required, type="text" name="title", maxlength=80)
		input(placeholder="설명", required, type="text" name="description", minlength=2)
		input(placeholder=",로 구분된 해시태그", required, type="text" name="hashtags")
		div
			button#actionBtn(disabled) 녹화하기
			input(type="submit" value="업로드")

block scripts
	script(src="/static/js/recorder.js") 
