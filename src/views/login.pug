extends base


block content
	if errorMessage
		span=errorMessage
	form(method="POST").login__form
		input(placeholder="아이디" name="username" type="text", required)
		input(placeholder="비밀번호" name="password" type="password", required)
		div
			a(href="/join").create__account 계정 만들기
			input(type="submit" value="로그인").login
		include partials/social-login