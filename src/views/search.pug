extends base.pug
include mixins/video

block content
	form.search__form
		input(placeholder="검색" name="keyword" type="text")
		button
			i.fas.fa-search

	div.video-grid
		each video in videos 
			+video(video)
		else
			span.empty__message 아직 업로드된 비디오가 없습니다.